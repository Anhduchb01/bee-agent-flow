---
description: Push the bee/* branch and open a draft PR — evidence-first, snapshots embedded by default
---

Invoke the bee-push-pr skill. If a PR is already open for this branch,
invoke bee-update-pr instead.

Follow the skill end-to-end: capture evidence FIRST (green run only,
screenshots committed to .bee/evidence/, mobile 390px shot included for UI
changes), then open the draft PR with the skill's template — Snapshot and
Demo/Preview sections filled in, never silently dropped.

$ARGUMENTS — optional: issue number to close, or notes for the PR body.
